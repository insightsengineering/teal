% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_teal.R
\name{.sidebar_overlay}
\alias{.sidebar_overlay}
\title{Create Sidebar Overlay Widget}
\usage{
.sidebar_overlay(id, toggle_ui, ui_content, shown = FALSE, direction = "left")
}
\arguments{
\item{id}{(\code{character(1)}) Unique identifier for the sidebar overlay element.
Used to target the sidebar for programmatic show/hide operations.
This id can later be used to perform actions on the sidebar with the help of the helper functions:
\itemize{
\item \code{.hide_sidebar_overlay(id)}
\item \code{.show_sidebar_overlay(id)}
\item \code{.glow_sidebar_overlay_close(id)}
\item \code{.enable_sidebar_overlay_close(id)}
}}

\item{toggle_ui}{(\code{shiny.tag}) UI element that will trigger the sidebar to open
when clicked. Typically an \code{actionButton} or similar interactive element.}

\item{ui_content}{(\code{shiny.tag} or \code{tagList}) The content to display inside the
sidebar panel. Can be any valid Shiny UI elements.}

\item{shown}{(\code{logical(1)}) Whether the sidebar should be initially visible.
Defaults to \code{FALSE} (hidden).}

\item{direction}{(\code{character(1)}) Side of the screen from which the sidebar slides.
Must be either \code{"left"} or \code{"right"}. Defaults to \code{"left"}.}
}
\description{
Generates a slide-out sidebar overlay that can be toggled open and closed.
The sidebar slides in from the left or right side of the screen and contains
custom UI content with a close button.
}
\details{
This function creates a complete sidebar overlay system with the following components:
\itemize{
\item A toggle ui (provided as input) that opens the sidebar when clicked
\item A sidebar panel that slides in from the specified direction
\item A close button (X) within the sidebar to hide it
\item Automatic CSS class management for show/hide animations
\item Required CSS dependencies for styling and animations
}

The sidebar uses CSS classes to control visibility and animations:
\itemize{
\item \code{teal-custom-sidebar}: Base styling for the sidebar
\item \code{show}: Controls visibility (added/removed to show/hide)
\item \code{left}/\code{right}: Controls which side the sidebar slides from
}

JavaScript click handlers are attached inline to manage the show/hide behavior
without requiring additional server-side logic.
}
\keyword{internal}
