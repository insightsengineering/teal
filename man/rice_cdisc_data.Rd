% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DataConnector.R
\name{rice_cdisc_data}
\alias{rice_cdisc_data}
\title{Data connector for \code{RICE}}
\usage{
rice_cdisc_data(..., code = character(0), additional_ui = NULL)
}
\arguments{
\item{...}{(\code{DatasetConnector}) dataset connectors created using \link{rice_dataset}}

\item{code}{optional, (\code{character}) preprocessing code}

\item{additional_ui}{\code{shiny.tag} additional user interface to be visible over login panel}
}
\value{
An object of class \code{DataConnector}
}
\description{
Build data connector for \code{RICE} datasets
}
\examples{
\dontrun{
x <- rice_cdisc_data(
  ADSL = rice_dataset("ADSL", "/path/to/ADSL"),
  ADLB = rice_dataset("ADLB", "/path/to/ADLB")
)
app <- init(
  data = x,
  modules = root_modules(
    module(
      "ADSL AGE histogram",
      server = function(input, output, session, datasets) {
        output$hist <- renderPlot({
          hist(datasets$get_data("ADSL", filtered = TRUE, reactive = TRUE)$AGE)
        })
      },
      ui = function(id, ...) {ns <- NS(id); plotOutput(ns('hist'))},
      filters = "ADSL"
    )
  ),
  filter = NULL,
  header = tags$h1("Sample App")
)
shinyApp(app$ui, app$server)
}
}
