# Extract all `transformators` from `modules`.

Extract all `transformators` from `modules`.

## Usage

``` r
extract_transformators(modules)
```

## Arguments

- modules:

  `teal_modules` or `teal_module`

## Value

A list of `teal_transform_module` nested in the same way as input
`modules`.
